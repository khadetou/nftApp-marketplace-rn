import React, {useState} from 'react';
import {Text, View} from 'react-native';

import {COLORS, NFTData} from '../constants';

const Home = () => {
  return (
    <View>
      <Text> This the homepage</Text>
    </View>
  );
};

export default Home;
